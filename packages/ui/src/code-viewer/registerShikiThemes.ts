/**
 * Weft Shiki theme names.
 *
 * Currently uses Shiki built-in themes for syntax highlighting.
 * When custom Weft themes are implemented, update these constants
 * and register them in registerWeftShikiThemes() below.
 */
export const WEFT_SHIKI_THEME_DARK = 'github-dark'
export const WEFT_SHIKI_THEME_LIGHT = 'github-light'

export function registerWeftShikiThemes(): void {
  // Stub — when custom themes are implemented, update the constants above
  // and register them with Shiki here.
}