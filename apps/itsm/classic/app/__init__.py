"""ITSM classic — a FastAPI ITSM SaaS (the skill's 2nd demo scaffold source).

Plain SaaS: incidents / changes / CIs / SLAs, two state machines, 3-role auth,
a live SSE feed. No weft, no AI — the agent layer is added by the
`integrate-weft-kit` skill on top of this (see ../../agentic).
"""
