/**
 * block-markers — stub for annotation block marker DOM utilities.
 */

import type { AnnotationV1 } from '@weft/core'

export function clearBlockAnnotationMarkers(_container: HTMLElement): void {
  // Stub — full implementation will be ported from the upstream source project
}

export function applyBlockAnnotationMarker(
  _container: HTMLElement,
  _annotationOrId: string | AnnotationV1,
  _blockId?: string,
): void {
  // Stub — full implementation will be ported from the upstream source project
}