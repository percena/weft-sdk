/**
 * remarkCollapsibleSections — stub remark plugin for collapsible sections.
 */

import type { Plugin } from 'unified'

const remarkCollapsibleSections: Plugin = () => {
  // Stub — no transformation
  return (tree: any) => tree
}

export default remarkCollapsibleSections