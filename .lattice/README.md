# `.lattice/` (Lattice binders)

Active Specs, Reviews, and ticket binders for this repo.

- **Ready-check:** agents run `ensure-lattice.sh` (not a user command)
- **Config:** `config.yaml` (`profile: strict|light`)
- **Bloodline:** Spec/binder edges + GitHub `Fixes`/`Refs` (ADR-010). No derived global index or BOARD (ADR-010).

See skill `start-work` portable policy and monorepo `docs/adopting.md` when available.
