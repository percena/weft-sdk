/**
 * Layout constants for chat UI
 *
 * Shared layout configuration for consistent spacing across the chat panel.
 */

export const CHAT_LAYOUT = {
  /** Max width constraint for chat content */
  maxWidth: 'max-w-[720px]',
  /** Container padding */
  containerPadding: 'px-4',
  /** Message vertical spacing */
  messageSpacing: 'space-y-4',
  /** User message padding */
  userMessagePadding: 'py-1',
} as const;

